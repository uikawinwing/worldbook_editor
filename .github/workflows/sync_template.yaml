name: legacy_template_sync_disabled

# Template/toolchain updates are centralized in TavernDev/Toolchain.
# Kept temporarily in the local pilot because CodexPro 8787 does not allow git rm.
# Remove this file when the shared-toolchain migration is committed to GitHub.
on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Template updates are handled by the shared TavernHelper Toolchain."
