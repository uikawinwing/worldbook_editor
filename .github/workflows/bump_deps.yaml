name: legacy_bump_deps_disabled

# Dependency updates are owned by the shared TavernDev/Toolchain.
# Kept temporarily in the local pilot because CodexPro 8787 does not allow git rm.
# Remove this file when the shared-toolchain migration is committed to GitHub.
on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Dependency updates are handled by the shared TavernHelper Toolchain."
